<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pw" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pw">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen">
    <% unless user_signed_in? %>
      <nav class="sticky top-0 z-50 w-full glass-card !rounded-none !border-x-0 !border-t-0 py-4 px-6 mb-8">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
          <%= link_to "#", class: "text-2xl font-bold tracking-tight hover:text-accent transition-colors" do %>
            Cameron <span class="text-accent underline decoration-accent/20 underline-offset-8">Kroupa</span>
          <% end %>
          
          <div class="hidden md:flex gap-8 items-center font-medium">
            <%= link_to "About", "#about", class: "hover:text-accent transition-colors" %>
            <%= link_to "Skills", "#skills", class: "hover:text-accent transition-colors" %>
            <%= link_to "Experience", "#experience", class: "hover:text-accent transition-colors" %>
            <%= link_to "Projects", "#projects", class: "hover:text-accent transition-colors" %>
            <%= link_to "Education", "#education", class: "hover:text-accent transition-colors" %>
            <%= link_to "Contact", "#contact", class: "hover:text-accent transition-colors" %>
          </div>
        </div>
      </nav>
    <% end %>

    <% if notice %>
      <div class="fixed top-4 right-4 z-50 glass-card px-6 py-3 text-green-800">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="fixed top-4 right-4 z-50 glass-card px-6 py-3 text-red-800">
        <%= alert %>
      </div>
    <% end %>

    <div class="container mx-auto px-4">
      <%= yield %>
    </div>
  </body>
</html>
