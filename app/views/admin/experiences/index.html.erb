<p style="color: green"><%= notice %></p>

<% content_for :title, "Experiences" %>

<h1>Experiences</h1>

<div id="experiences">
  <% @experiences.each do |experience| %>
    <%= render experience %>
    <p>
      <%= link_to "Show this experience", [:admin, experience] %>
    </p>
  <% end %>
</div>

<%= link_to "New experience", new_admin_experience_path %>
