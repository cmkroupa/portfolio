<div id="<%= dom_id skill %>" class="glass-card flex items-center gap-4 p-4 h-full group">
  <div class="w-10 h-10 flex-shrink-0 flex items-center justify-center bg-white/40 rounded-xl border border-white/20 shadow-sm group-hover:scale-110 transition-transform duration-300">
    <% if skill.icon.attached? %>
      <%= image_tag skill.icon, class: "w-6 h-6 object-contain" %>
    <% else %>
      <span class="text-xl opacity-80">🛠️</span>
    <% end %>
  </div>
  <div class="flex flex-col leading-tight overflow-hidden">
    <span class="font-bold text-sm truncate"><%= skill.title %></span>
    <span class="text-[10px] opacity-60 truncate"><%= skill.tag %></span>
  </div>
</div>
